<!DOCTYPE html SYSTEM "http://www.thymeleaf.org/dtd/xhtml1-strict-thymeleaf-4.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:th="http://www.thymeleaf.org">

    <head>
        <title>User's tasks</title>
        <meta charset="utf-8" />
    </head>

    <body>
        <h1 th:text="${drinkki.nimi}">Drinkin nimi</h1>
	
        <h2>Käytetyt raaka-aineet</h2>
  	

	<ul>
            <li th:each="aine : ${raakaaineet}">
                <span th:text="${aine.nimi}">Task</span>
		<span th:text="${aine.juomaanLisattavaMaara}">Task</span>
		<
		
            </li>
        </ul>
	
	<h2>Muut tiedot</h2>
	
	<ul>
            <li th:each="drinkki : ${drinkki}">
                <span th:text="${drinkki.lasityyppi}">Task</span>
		<br/>
		<span th:text="${drinkki.resepti}">Task</span>
		<br/>
		<span th:text="${drinkki.hinta}">Task</span>
		<br/> 
		<span th:text="${drinkki.alkoholipitoisuus}">Task</span>
		
		
		
            </li>
        </ul>

    </body>
</html>
